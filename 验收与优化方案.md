# 搅拌站报价助手：优化与功能验收

## 一、项目判断

压缩包内是 Next.js 16 + React 19 的移动端网页原型，入口为 `_source/app/page.tsx`，并非微信开发者工具可直接导入的原生小程序工程。当前数据来自 `_source/lib/data.ts`，报价、站点、通知均为演示数据。

因此本轮验收分为两层：

1. 验收当前网页原型的界面、交互和异常反馈。
2. 标记迁移为微信小程序以及接入真实业务后才能验收的能力。

## 二、本轮已完成

- 注册资料：姓名、手机号、地址必填校验，手机号格式校验，错误信息显示在对应字段下方。
- 会话保持：注册完成后将身份与资料保存到浏览器本地；刷新页面仍能回到首页；退出登录会清除本地资料。
- 报价中心：全部 / 本周 / 本月筛选可用，报价详情可打开和关闭。
- 询价闭环：报价详情新增预计用量、备注和提交按钮；空用量会拦截，提交后展示确认状态。当前明确标注为演示环境，不会伪装成真实下单。
- 站点定位：重新定位按钮会调用浏览器定位能力；成功、未授权和不支持定位都有反馈，不影响继续浏览资料地址匹配的站点。
- 可访问性：主要按钮、输入框和弹层增加键盘焦点样式；输入框补充姓名、电话和地址语义；支持减少动效偏好。
- 构建质量：移除 `typescript.ignoreBuildErrors`，生产构建可以真正执行 TypeScript 检查。

## 三、验收结果

| 验收项 | 结果 | 说明 |
| --- | --- | --- |
| 首屏加载与移动端布局 | 通过 | 本地开发服务首页返回 HTTP 200，页面按 430px 手机壳布局渲染 |
| 未选择身份不能继续 | 通过 | 入口按钮保持禁用状态 |
| 注册必填与手机号校验 | 通过 | 姓名、11 位手机号、地址均有校验和错误提示 |
| 注册后进入首页 | 通过 | 身份和资料进入首页欢迎区，并写入本地会话 |
| 刷新后保持会话 | 通过 | 通过 `localStorage` 恢复有效资料 |
| 报价筛选与详情弹层 | 通过 | 筛选数量同步变化，详情可关闭 |
| 提交询价需求 | 通过（演示） | 表单校验、提交确认可用；未接真实接口 |
| 站点定位 | 部分通过 | 浏览器端有定位反馈；未接微信 `wx.getLocation` 与逆地理编码 |
| 站点导航 | 部分通过 | 当前使用高德 URI 链接；需在真实手机和已安装地图应用上确认拉起行为 |
| 真实报价、通知、账户设置 | 未通过 / 待接入 | 当前是本地静态演示数据，不能作为上线验收依据 |
| 微信小程序兼容性 | 阻塞 | 需要将 Next.js 页面迁移为 WXML / WXSS / JS 或选定跨端框架后再验收 |

## 四、上线前方案

### 1. 先确定技术路线

如果目标是提交微信审核，应建立原生小程序或明确的 Taro / uni-app 工程，页面拆分为 `launch`、`register`、`home`、`quotes`、`profile` 五个页面。当前 Next.js 工程可以作为交互原型和视觉参考，不能直接替代小程序工程。

### 2. 业务接口

- `POST /auth/login`：微信登录换取业务会话。
- `GET /stations/nearby`：根据经纬度和筛选条件返回站点。
- `GET /quotes`：按时间范围、标号和站点查询报价。
- `POST /inquiries`：提交站点、标号、预计用量、备注和联系人。
- `GET /notifications`：返回用户消息并支持已读状态。

### 3. 微信能力替换

- 定位使用 `wx.getLocation`，站点导航使用 `wx.openLocation`；在 `app.json` 配置 `scope.userLocation` 的用途说明。
- 资料缓存使用 `wx.setStorageSync` / `wx.getStorageSync`，退出时清理用户态数据。
- 真实手机号应通过微信登录与后端会话完成绑定，不应把手机号校验当作身份认证本身。
- 地图使用小程序 `map` 组件或合规地图插件，不能把当前网页 Leaflet 高德瓦片方案原样带入小程序。

### 4. 发布前必测

- 微信开发者工具：模拟器、真机、弱网、无定位权限、拒绝授权后重新授权。
- iOS / Android：输入法遮挡、返回键、底部安全区、地图拉起、电话拨打。
- 数据一致性：重复提交、接口超时、报价为空、站点无结果、登录过期。
- 隐私合规：定位用途、手机号用途、服务协议和隐私政策使用真实内容，不使用演示文案。

## 五、参考依据

- [微信小程序设计指南](https://developers.weixin.qq.com/miniprogram/design/)：重点突出、导航明确、及时反馈。
- [微信位置能力说明](https://developers.weixin.qq.com/miniprogram/dev/api/location/wx.getLocation.html)：定位需用户授权，并根据平台规则配置权限说明。
- [微信打开位置](https://developers.weixin.qq.com/miniprogram/dev/api/location/wx.openLocation.html)：用于在微信内置地图查看站点。
- [高德 URI API](https://lbs.amap.com/api/uri-api/summary)：网页或应用拉起高德地图的参数约定。
- [Next.js 静态导出](https://nextjs.org/docs/app/guides/static-exports)：当前工程可作为静态网页部署，但静态导出不提供微信原生运行时能力。

## 六、验证命令

在 `_source` 目录执行：

```text
pnpm install --frozen-lockfile
pnpm run build
```

本轮结果：`pnpm run build` 通过，TypeScript 检查通过，首页本地请求返回 HTTP 200。
